{%- assign selected_variant = prod.selected_or_first_available_variant -%}
{%- assign render_style = settings.product_options_style | default: 'radios' -%}

<div class="js-product-options u-hide">
  <script type="application/json">
    {
      "product": {{ prod | json }},
      "optionsWithValues": {{ prod.options_with_values | json }},
      "selectedVariantId": {{ selected_variant.id | json }},
      "settings": {
        "renderStyle": {{ render_style | json }}
      }
    }
  </script>
  <div class="js-product-options-render-target">
    <input type="hidden" name="id" value="{{ selected_variant.id }}" />
  </div>
</div>
