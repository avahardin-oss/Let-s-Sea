{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/snippets/category-slider-card/snippet.liquid}] %}

<div class="portable-snippet" data-snippet-name="category-slider-card">

<style>
.mod_category_slider_card_Leq4bq_CategorySliderCard{display:contents}
.mod_category_slider_card_Leq4bq_CategorySliderCard__image{background-position:50%;background-repeat:no-repeat;background-size:contain;width:max(2.5rem,2.77778vw);min-width:max(2.5rem,2.77778vw);height:max(2.5rem,2.77778vw)}
.mod_category_slider_card_Leq4bq_CategorySliderCard__image--mobile{aspect-ratio:1;width:28vw;height:auto;min-width:unset;background-position:50%;background-repeat:no-repeat;background-size:contain}
.mod_category_slider_card_Leq4bq_CategorySliderCard__title{text-align:left;-webkit-line-clamp:2;text-overflow:ellipsis;-webkit-box-orient:vertical;margin-bottom:0;font-size:max(.875rem,.972222vw);font-weight:600;display:-webkit-box;overflow:hidden}
.mod_category_slider_card_Leq4bq_CategorySliderCard__title--mobile{text-align:center;-webkit-line-clamp:2;text-overflow:ellipsis;-webkit-box-orient:vertical;margin-bottom:0;font-size:max(.875rem,.972222vw);font-weight:600;display:-webkit-box;overflow:hidden}
@media print,screen and (width>=40em){.mod_category_slider_card_Leq4bq_CategorySliderCard__image--mobile{width:17vw}
}
@media print,screen and (width>=64em){.mod_category_slider_card_Leq4bq_CategorySliderCard__image{width:max(8.75rem,9.72222vw);height:max(8.75rem,9.72222vw)}
.mod_category_slider_card_Leq4bq_CategorySliderCard__title{font-size:max(1rem,1.11111vw)}
}
</style>

{%- liquid
  assign title = title | default: blank
  assign url = url | default: blank
  assign image = image | default: blank
  assign image_alt = image_alt | default: blank
  assign fallback_image = fallback_image | default: blank
  assign fallback_title = fallback_title | default: blank
  assign variant = variant | default: 'default'

  assign display_image = blank
  if image != blank
    assign display_image = image
  elsif fallback_image != blank
    assign display_image = fallback_image
  endif

  assign display_title = blank
  if title != blank
    assign display_title = title
  elsif fallback_title != blank
    assign display_title = fallback_title
  endif

  assign alt_text = ''
  if image != blank and image_alt != blank
    assign alt_text = image_alt
  elsif image == blank and fallback_image != blank and fallback_image.alt != blank
    assign alt_text = fallback_image.alt
  elsif title != blank
    assign alt_text = title
  elsif fallback_title != blank
    assign alt_text = fallback_title
  endif
-%}

{%- if url != blank -%}
  <a href="{{ url }}">
    {%- if display_image != blank -%}
      {%- capture image_srcset -%}
        {%- render 'print-image-sizes', image: display_image -%}
      {%- endcapture -%}

      {%- if variant == 'mobile' -%}
        <div
          class="mod_category_slider_card_Leq4bq_CategorySliderCard__image--mobile lazyload"
          role="img"
          aria-label="{{ alt_text | escape }}"
          data-sizes="auto"
          data-alt="{{ alt_text | escape }}"
          data-bgset="{{ image_srcset | strip_newlines | strip }}"
        ></div>
      {%- else -%}
        <div
          class="mod_category_slider_card_Leq4bq_CategorySliderCard__image lazyload"
          role="img"
          aria-label="{{ alt_text | escape }}"
          data-sizes="auto"
          data-alt="{{ alt_text | escape }}"
          data-bgset="{{ image_srcset | strip_newlines | strip }}"
        ></div>
      {%- endif -%}
    {%- endif -%}

    {%- if variant == 'mobile' -%}
      <h3 class="mod_category_slider_card_Leq4bq_CategorySliderCard__title--mobile">{{ display_title }}</h3>
    {%- else -%}
      <h3 class="mod_category_slider_card_Leq4bq_CategorySliderCard__title">{{ display_title }}</h3>
    {%- endif -%}
  </a>
{%- endif -%}

</div>
